# PyQt Monitoring 환경변수 템플릿
# 실제 값은 .env.local 또는 셸에서 export.

# Management gRPC
MANAGEMENT_GRPC_HOST=localhost
MANAGEMENT_GRPC_PORT=50051

# EventGateway gRPC target — HANDOFF_ACK / PP_DONE_REQUESTED / ITEM_LOOKUP_REQUESTED publish 용
EVENT_GATEWAY_TARGET=localhost:50051
